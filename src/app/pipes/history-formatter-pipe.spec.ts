import { HistoryFormatterPipe } from './history-formatter-pipe';

describe('HistoryFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new HistoryFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
